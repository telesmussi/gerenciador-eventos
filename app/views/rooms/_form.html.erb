<%= semantic_form_for([@event, @room], :html => {:class => "form-horizontal"}) do |f| %>
<%= f.inputs do %>
<%= f.input :name %>
<%= f.input :price, :label => "Preço (R$)" %>
<%= f.input :limit %>
<% end %>
<%= f.buttons %>

<% end %>
